#include <stdio.h>

int main() {
  int suspect1 = 1; // 1 = มีรอยขีดข่วน, 0 = ไม่มี
  int suspect2 = 1; // 1 = มีประวัติอาชญากรรม, 0 = ไม่มี
  int suspect3 = 1; // 1 = หายตัวไป, 0 = อยู่

	int ans = suspect1 + suspect2 + suspect3;
	
	if (ans = 3)
	{
		printf("suspect\n");
	}
	else
	{
		printf("not suspect\n");
	}


 
  // ตรวจสอบหลักฐานของผู้ต้องสงสัย 2
  

  // ตรวจสอบหลักฐานของผู้ต้องสงสัย 3
  
  // ไม่มีหลักฐานเพียงพอที่จะตัดสินผู้ต้องหาคนใดคนหนึ่ง
  

  return (0);
}
